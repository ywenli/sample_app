<!-- 既存のリレーションシップを見つけ出す-->
<!-- Ajaxを使ってページ移動せずにフォロー解除する-->
<%= form_for(current_user.active_relationships.find_by(
  followed_id: @user.id),
  html: { method: :delete },
  remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>